{
	# Set a real email via ACME_EMAIL at runtime.
	email {$ACME_EMAIL}
	acme_ca https://acme-v02.api.letsencrypt.org/directory
}

# Import reusable snippets and matchers globally
import /sites/_snippets.inc
import /sites/_matchers.inc

# Import all site configurations
import /sites/*.caddy
